# Static assets

Drop static files here. They are served from the site root.

Expected:

- `og-default.png` — 1200×630 social-share image. Replace the placeholder
  whenever the visual identity changes.
- `resume.pdf` — downloadable resume (linked from the Resume app).
- `favicon.ico`, `apple-touch-icon.png` — generated/replaced as you like.

This file is just documentation and is safe to delete or ignore.
